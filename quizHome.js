/*only stores the function to move from this page to the next one, just wanted to give each file its own js and not complicate things*/
function goToNextPage(nextPage) {
  window.location.href = nextPage;
}
